import StubPage from "@/components/StubPage";

export default function MarketPage() {
  return (
    <StubPage title="MCPs Add-Ons" subtitle="Browse community templates beyond the built-in catalog" icon="store">
      Future: pull-to-install templates from a public registry, with version pinning and signature verification.
    </StubPage>
  );
}
